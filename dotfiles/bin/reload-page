#!/bin/bash

BROWSER=Chromium

MYWINDOW=$(xdotool getactivewindow)
xdotool search --onlyvisible --class ${BROWSER} windowfocus key 'ctrl+r'
xdotool windowfocus --sync ${MYWINDOW}
xdotool windowactivate --sync ${MYWINDOW}